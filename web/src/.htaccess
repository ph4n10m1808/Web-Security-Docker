# Prevent directory listing

Options -Indexes

# Allow access to specific file types (optional)

<FilesMatch "\.(html|css|js|png|jpg|jpeg|gif|php)$">
Order Allow,Deny
Allow from all
</FilesMatch>

# Custom 403 Forbidden error message

ErrorDocument 403 "Access Forbidden"
